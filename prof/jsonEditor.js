const fs = ('fs');
var argv = process.argv.slice(2);

/*
function insults (file, property, content)
{
  var obj = fs.readFileSync(file);
  obj[property] += content;
}

function getString (argv)
{
  let string = '';

  for (var i = 2; i <= argv.length; i++);
  {
    string += argv[i];
    string += ' ';
    console.log(string);
  }
  return string;
}

var array = ['borsetto.json', 'Borsetto', 'c', 'a', 'n', 'c', 'r', 'o'];

console.log(getString(argv));

//insults(argv[0], argv[1], getString(argv));
*/

console.log(argv.lenght);